buildscript {
    repositories {
        maven { url 'https://maven.architectury.dev/' } // これが必須
        mavenCentral()
    }
    dependencies {
        classpath 'dev.architectury:architectury-plugin:3.4.140' // ←存在するバージョン
    }
}

apply plugin: 'architectury-plugin'

architectury {
    common(['fabric', 'forge'])
}

dependencies {
    compileOnly "dev.architectury:architectury:6.5.85"
}

java {
    toolchain.languageVersion.set(JavaLanguageVersion.of(17))
}
