plugins {
    id 'architectury-plugin' version '3.4.139'
}

architectury {
    common(['fabric', 'forge'])
}

dependencies {
    compileOnly "dev.architectury:architectury:6.5.85"
}

java {
    toolchain.languageVersion.set(JavaLanguageVersion.of(17))
}

repositories {
    maven {
        url = uri("https://maven.architectury.dev/")
    }
    mavenCentral()
}
