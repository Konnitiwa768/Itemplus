pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = uri("https://maven.architectury.dev/")
        }
        mavenCentral()
    }
}
rootProject.name = 'sakaplus'

include 'common', 'fabric', 'forge'

